# database init, supports mysql too
database=h2
spring.sql.init.schema-locations=classpath*:db/${database}/schema.sql
spring.sql.init.data-locations=classpath*:db/${database}/data.sql

# Web
spring.thymeleaf.mode=HTML

# JPA
spring.jpa.hibernate.ddl-auto=none
spring.jpa.open-in-view=false
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategySnakeCaseImpl
spring.jpa.properties.hibernate.default_batch_fetch_size=16

# Internationalization
spring.messages.basename=messages/messages

# Actuator
# Expose all actuator endpoints for monitoring and management purposes
# Don't do this in production, only for development and testing
management.endpoints.web.exposure.include=*

# Logging
logging.level.org.springframework=INFO
# logging.level.org.springframework.web=DEBUG
# logging.level.org.springframework.context.annotation=TRACE

# Maximum time static resources should be cached
spring.web.resources.cache.cachecontrol.max-age=12h

# Spring AI — Azure OpenAI endpoint with managed identity (Microsoft Foundry)
# Set AZURE_OPENAI_ENDPOINT and AZURE_OPENAI_DEPLOYMENT environment variables.
# No API key is used; authentication is via managed identity (DefaultAzureCredential).
# spring.ai.openai.microsoft-foundry and spring.ai.openai.microsoft-deployment-name
# bind to OpenAiCommonProperties (prefix spring.ai.openai) — verified against
# spring-ai-autoconfigure-model-openai 2.0.1 bytecode; OpenAiAutoConfigurationUtil
# reads isMicrosoftFoundry() from the common-properties instance.
spring.ai.openai.base-url=${AZURE_OPENAI_ENDPOINT:https://models.inference.ai.azure.com}
spring.ai.openai.microsoft-foundry=${AZURE_OPENAI_MICROSOFT_FOUNDRY:true}
spring.ai.openai.microsoft-deployment-name=${AZURE_OPENAI_DEPLOYMENT:gpt-5-4-mini}
spring.ai.openai.model=${AZURE_OPENAI_DEPLOYMENT:gpt-5-4-mini}
spring.ai.openai.chat.model=${AZURE_OPENAI_DEPLOYMENT:gpt-5-4-mini}
spring.ai.openai.timeout=60s
spring.ai.openai.max-retries=2
